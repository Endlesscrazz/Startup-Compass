"use client";

import { AtlasHeader } from "@/components/AtlasPages";

/** @deprecated Use AtlasHeader — kept for imports that expect `Header`. */
export function Header() {
  return <AtlasHeader />;
}
